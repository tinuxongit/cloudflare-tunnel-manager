[workspace]
resolver = "2"
members = ["core", "src-tauri"]

# Strip + LTO for release; useful both for the Tauri app and the future
# connector binary. Inherited by all members unless overridden.
[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
