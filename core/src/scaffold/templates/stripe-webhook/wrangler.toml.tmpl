name = "{{project_name}}"
main = "src/index.js"
compatibility_date = "2025-01-01"
workers_dev = {{workers_dev}}

# STRIPE_WEBHOOK_SECRET is required. Set it via Cloudflare Studio → Workers →
# this Worker → Secrets, or `wrangler secret put STRIPE_WEBHOOK_SECRET`.
