# {{project_name}}

Empty Worker, deployed via Cloudflare Studio.

## Live URL

After deploy: `https://{{project_name}}.<your-cf-subdomain>.workers.dev`

## Local dev

```sh
pnpm install
pnpm run dev          # http://localhost:8787
```

## Push changes

```sh
pnpm run deploy
```
