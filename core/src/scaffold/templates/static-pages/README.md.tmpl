# {{project_name}}

Static site on Cloudflare Pages, deployed via Cloudflare Studio.

## Live URL

After deploy: `https://{{project_name}}.pages.dev`

## Edit

Put files in `public/` — anything in there gets served as-is. `index.html` is the home page.

## Push

```sh
pnpm run deploy
```

Or click "Deploy" on this project in Cloudflare Studio.
